%br/
%br/
= image_tag @user.photo.url(:thumb)
%br/
%br/
= @user.name
%br/
%br/
= render 'follow_form' if signed_in?
%br/
= render 'users/stats' if signed_in?
%br/

= link_to 'Edit', edit_user_path(@user) if current_user == @user.id